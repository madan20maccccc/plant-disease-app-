services:
  - type: web
    name: plant-disease-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
```

**Step 2 — Update `requirements.txt`** to also include setuptools:
```
setuptools==68.0.0
flask==2.3.3
numpy==1.24.3
tensorflow==2.13.0
pillow==10.0.0
werkzeug==2.3.7
gunicorn==21.2.0